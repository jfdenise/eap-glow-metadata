# layers-config-gen tools

* This tool can't be run in PNC build, it MUST be run prior to tag a new release.
* A script and maven project to generate the layers config that need to be commited into the metadata sources.
